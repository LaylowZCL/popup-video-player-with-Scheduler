{
  "name": "popup-video-player-with-scheduler",
  "version": "1.0.0",
  "description": "Aplicação de agendamento com reprodução de vídeos em popup",
  "author": "Fernando Zucula",
  "license": "MIT",
  "main": "src/main.js",

  "scripts": {
    "start": "electron .",
    "dev": "electron . --inspect=5858",
    "debug": "electron . --remote-debugging-port=9222",
    "dist": "electron-builder --win",
    "dist:all": "electron-builder -w -m -l"
  },

  "dependencies": {
    "axios": "^1.6.0"
  },

  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "^24.13.3"
  },

  "build": {
    "appId": "com.popup.videoplayer.scheduler",
    "productName": "Popup Video Scheduler",
    "asar": true,
    "npmRebuild": false,
    "nodeGypRebuild": false,
    "buildDependenciesFromSource": false,
    "forceCodeSigning": true,

    "directories": {
      "output": "dist",
      "buildResources": "assets"
    },

    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],

    "win": {
      "target": ["nsis", "msi"],
      "certificateFile": "C:\\Users\\ferna\\Desktop\\certificado.pfx",
      "certificatePassword": "senhaDoTeste",
      "verifyUpdateCodeSignature": false
    },

    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Popup Video Scheduler"
    },

    "msi": {
      "oneClick": false,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}
